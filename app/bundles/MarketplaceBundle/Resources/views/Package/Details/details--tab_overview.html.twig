<div class="card mt-sm">
    <div class="card-header">
        <div class="card-title">{% trans %}marketplace.package.cli.install{% endtrans %}</div>
    </div>
    <div class="card-body">{{ 'marketplace.package.cli.install.descr'|trans({
        '%vendor%' : packageDetail.packageBase.getVendorName(),
        '%package%' : packageDetail.packageBase.getPackageName(),
    })|purify }}</div>
</div>