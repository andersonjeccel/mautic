{% block _formaction_properties_row %}
<div class="row">
    <div class="col-6">
        {% for child in form.children %}
            <div class="form-group col-6">
                {{ form_label(child) }}
                {{ form_widget(child) }}
                {{ form_errors(child) }}
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
