{% block emailsend_list_row %}
<div class="row">
    <div class="col-12 pe-0">
        {{ form_row(form.email) }}
        <div class="mb-lg d-flex">
            {{ form_row(form.newEmailButton) }}
            {{ form_row(form.editEmailButton) }}
            {{ form_row(form.previewEmailButton) }}
        </div>
    </div>

    {% if form.email_type is defined %}
    <div class="col-6">
        {{ form_row(form.email_type) }}
    </div>
    {% endif %}
    <div class="col-6 hide" id="email_send_to_dnc_status">
        <span class="col-form-label d-block" data-bs-toggle="tooltip" data-bs-container="body" data-bs-placement="top"
            data-bs-title="{{ 'mautic.email.send.dnc.tooltip.info'|trans }}">
            {{ 'mautic.email.send.dnc.label'|trans }}
            <i class="fa fa-question-circle"></i>
        </span>
        <span class="dnc-status-text badge"></span>
    </div>
    {% if form.priority is defined %}
    <div id="priority" class="col-6 queue_hide">
        {{ form_row(form.priority) }}
    </div>
    {% endif %}
    {% if form.attempts is defined %}
    <div id="attempts" class="col-6 queue_hide">
        {{ form_row(form.attempts) }}
    </div>
    {% endif %}
</div>
{% endblock %}