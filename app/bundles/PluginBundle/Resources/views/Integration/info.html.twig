{#
  Variables
    - bundle
    - icon
#}
<div class="row">
    <div class="col-4">
        <img class="img img-fluid" style="margin: auto;" src="{{ getAssetUrl(icon) }}" />
    </div>

    <div class="col-8">
        <h3>{{ bundle.primaryDescription|purify }}</h3>
    </div>
</div>

{% if bundle.hasSecondaryDescription %}
<div class="row mt-lg">
    <div class="col-12">
        {{ bundle.secondaryDescription|purify }}
    </div>
</div>
{% endif %}
