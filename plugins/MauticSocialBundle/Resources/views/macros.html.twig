{% macro profileImage(profile) %}
  {% if profile.profileImage is defined and profile.profileImage is not empty %}
    <div class="flex-shrink-0 me-3 card">
      <img src="{{ profile.profileImage }}" width="100px" class="rounded" />
    </div>
  {% endif %}
{% endmacro %}
