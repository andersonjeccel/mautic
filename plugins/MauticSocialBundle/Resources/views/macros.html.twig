{% macro profileImage(profile) %}
  {% if profile.profileImage is defined and profile.profileImage is not empty %}
    <div class="float-left card">
      <img src="{{ profile.profileImage }}" width="100px" class="media-object rounded" />
    </div>
  {% endif %}
{% endmacro %}
